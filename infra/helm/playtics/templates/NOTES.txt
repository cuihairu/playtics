Playtics deployed.
- Gateway: svc/playtics-gateway (port {{ .Values.service.gateway.port }})
- Control: svc/playtics-control (port {{ .Values.service.control.port }})

Assumes external Kafka ({{ .Values.env.kafkaBootstrap }}), Schema Registry ({{ .Values.env.registryUrl }}), and ClickHouse ({{ .Values.env.clickhouseUrl }}).

Update values.yaml accordingly and run:
  helm upgrade --install playtics ./infra/helm/playtics -n playtics --create-namespace
