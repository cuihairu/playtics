apiVersion: apps/v1
kind: Deployment
metadata:
  name: pit-control
  namespace: pit
spec:
  replicas: 1
  selector: { matchLabels: { app: pit-control } }
  template:
    metadata: { labels: { app: pit-control } }
    spec:
      containers:
        - name: control
          image: your-registry/pit-control:latest
          ports: [{containerPort: 8085}]
          env:
            - name: SPRING_CONFIG_ADDITIONAL_LOCATION
              value: file:/config/application.yaml
          volumeMounts:
            - name: cfg
              mountPath: /config
      volumes:
        - name: cfg
          configMap:
            name: pit-control-config
---
apiVersion: v1
kind: Service
metadata:
  name: pit-control
  namespace: pit
  annotations: {}
spec:
  type: ClusterIP
  selector: { app: pit-control }
  ports:
    - port: 8085
      targetPort: 8085
      name: http
